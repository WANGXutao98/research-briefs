---
date: 2026-06-15
type: progress_summary
generated_by: Night Research Agent 1
---

# 2026-06-15 — Research Progress Summary

## Today's Work

### GitHub Activity (Last 24 Hours)

No personal commits from David on June 14 or June 15.

**Important correction from previous summaries:** Prior progress reports (June 13 and June 14) incorrectly stated that `sana-wm-pipeline` was "paused since June 3." This was wrong — the GitHub public API was rate-limited during those runs, masking David's actual activity. The authenticated scan tonight reveals **10 personal commits on June 13** to `sana-wm-pipeline`. The true last-push date is June 13, not June 3.

### repo: WANGXutao98/research-briefs

All commits on June 14–15 are automated agent outputs:

| Commit Message | Time (UTC) |
|---------------|------------|
| `agent: github trending scan 2026-06-15` | 20:09 |
| `agent: blog scan 2026-06-15` | 19:06 |
| `agent: morning briefing 2026-06-14 [COMPLETE]` | 22:06 (June 14) |
| `agent: research opportunity report 2026-06-14` | 21:05 (June 14) |
| `agent: github trending scan 2026-06-14` | 20:08 (June 14) |
| `agent: blog scan 2026-06-14` | 19:04 (June 14) |
| `agent: progress summary 2026-06-14` | 17:04 (June 14) |

No manual annotations, notes, or planning documents added by David.

### repo: WANGXutao98/sana-wm-pipeline (corrected record)

**Last pushed: June 13** (2 days ago, not 12 days ago as previously reported).

David made **10 personal commits on June 13** across an ~15-hour session:

| Time (UTC) | Commit | Key Result |
|-----------|--------|-----------|
| 01:14 | `feat: GT-pose vs Default mode comparison experiment (DL3DV smoke test)` | GT-pose ATE 1.79e-7m (near-zero); Default SLAM ATE 0.127m (13cm drift); PSNR similar (11.17 vs 11.26 dB) |
| 01:18 | `docs: add comprehensive GT-pose vs Default mode experiment guide` | — |
| 01:41 | `chore: update vipe submodule to WANGXutao98/vipe fork` | SANA-WM patches for CachedDepthModel |
| 02:10 | `docs: add complete experiment reproduction guide (GT-pose vs Default)` | Commands verified on H100 |
| 06:19 | `feat(stage02): fix gt-depth mode + OmniWorld E2E pipeline` | GT-depth mode rewrite; OmniWorld-Game integration; 141 tests pass |
| 07:25 | `fix: run_e2e_gtdepth.sh — normalize_video, heredoc, GT depth 16fps resampling` | 3 bug fixes |
| 09:09 | `feat: adapt prepare_omniworld.py to actual ModelScope tar.gz format` | Complete rewrite for real OmniWorld data |
| 09:28 | `fix: run_e2e_gtdepth.sh — WebDataset schema naming + VIPE_EXT_JIT=1` | GT-depth smoke result: **ATE 9.07mm** |
| 15:43 | `Add OmniWorld pipeline validation docs and e2e experiment scripts` | — |
| 16:06 | `docs: rewrite REPRODUCTION_GUIDE.md — three-mode, two-dataset` | **GT-depth: ATE 9.07mm, PSNR 18.50dB, SSIM 0.730; Default: ATE 9.08mm, PSNR 18.84dB** |

**Summary of June 13 achievement:** David completed the OmniWorld dataset integration, built a GT-depth pipeline mode from scratch, fixed multiple pipeline bugs, and achieved validated end-to-end results across three modes (GT-pose, GT-depth, Default) on two datasets (DL3DV, OmniWorld). The pipeline now has 141 passing tests and fully reproducible results committed to docs.

---

## Problems / Blockers Encountered

**Active blockers detected from fix: and debug: commits (June 13):**

1. **WebDataset schema mismatch** (`fix: run_e2e_gtdepth.sh — correct WebDataset schema naming`) — tar entries were using subdir format (`{id}/file.npy`) but the verify_and_eval stage expected compound-extension format (`{id}.poses_c2w.npy`). Fixed June 13 09:28.

2. **VIPE slam_ext API mismatch** — Required `VIPE_EXT_JIT=1` env flag to use pre-built extensions; pre-built vs JIT API differed. Fixed in the same commit (June 13 09:28).

3. **normalize_video function name error** (`fix: run_e2e_gtdepth.sh — normalize_video`) — Called `normalize_clip` which didn't exist. Fixed June 13 07:25.

4. **heredoc bash variable expansion** — Using `<<'PYEOF'` (single-quoted) prevented bash from expanding env vars inside Python inline scripts, causing `os.environ KeyError`. Fixed to `<<PYEOF`. Fixed June 13 07:25.

5. **GT depth 16fps alignment** — GT depth and normalized video were at different framerates, causing per-frame misalignment in evaluation. Fixed by adding a GT-depth resampling stage (Stage 1b). Fixed June 13 07:25.

**Unresolved (pattern-based):**

- **WorldBench design doc not committed** — This was flagged as priority #1 for five consecutive days (June 11–15) by the morning briefing and progress agents. Today (June 15) is the explicitly stated "weekly deadline" from the June 14 morning briefing: *"By end of the week (Sunday June 15), have the WorldBench/WMATS design doc committed."* The doc has not appeared in any commit. This is not a technical blocker — it is a writing-to-committing threshold. The implementation and reading work exist; the synthesis document does not.

---

## Experiments In Progress

**Active (June 13, with concrete results):**

- **Three-mode pipeline comparison on sana-wm-pipeline** — GT-pose, GT-depth, Default modes are all running end-to-end. Results as of June 13 16:06:
  - GT-pose (DL3DV): ATE ~1.8e-7m (numerical zero — direct GT poses fed in, expected)
  - Default (DL3DV): ATE 0.127m (~13cm SLAM drift from VIPE; PSNR/SSIM not degraded because hallucination occurs in regions not captured by pixel metrics)
  - GT-depth (OmniWorld): ATE 9.07mm, PSNR 18.50dB, SSIM 0.730
  - Default (OmniWorld): ATE 9.08mm, PSNR 18.84dB
  - Key finding: **PSNR/SSIM do not capture semantic hallucination from SLAM drift** — GT-pose and Default have similar PSNR (11.17 vs 11.26 dB on DL3DV) despite a 714,000× difference in ATE. This is an empirically important result for the pipeline quality argument.

**Planned but not started:**

- **WorldBench WMATS no-WM baseline** — OpenEnv + MuJoCo peg insertion + Diffusion Policy. No commits indicate any setup has started. The June 14 morning briefing called for "one concrete WMATS number" by end of Sunday June 15.

- **Cosmos Predict 2.5 failure taxonomy** — No GPU session initiated. Still at reading/planning stage.

**Paused:**

- None currently. The sana-wm-pipeline is paused at the "next steps" decision point as of June 13 evening (the final commit is a documentation rewrite, signaling the session ended in a stable state).

---

## Current Key Research Questions

1. **What does the PSNR/SSIM ≈ ATE decorrelation in sana-wm-pipeline mean for world model evaluation?** The June 13 experiment showed GT-pose and Default modes produce nearly identical PSNR/SSIM on DL3DV despite a 714,000× ATE difference. This is a direct parallel to the WorldBench hypothesis: visual quality metrics (PSNR/SSIM for video quality, FVD for world model fidelity) may fail to capture the downstream-relevant failure modes (semantic hallucination, policy-relevant physical consistency). The sana-wm-pipeline result is empirical evidence for the same claim the WMATS benchmark is theoretically designed to make. These two projects are now arguing the same point from complementary angles.

2. **What is the next phase for sana-wm-pipeline after three-mode validation?** The pipeline now produces reproducible results in all three modes. The natural next question is: which mode produces the best downstream SANA-WM video generation quality for *embodied AI data annotation use cases* — where "quality" means semantic faithfulness to physical transitions, not PSNR. This requires a perceptual/semantic evaluation that goes beyond current metrics.

3. **Does visual world model fidelity (FVD, PSNR) actually improve policy performance?** The core WorldBench question. Now reinforced by the sana-wm-pipeline finding. Still unanswered at the experimental level because the WMATS baseline experiment has not started.

4. **What is the minimal experiment that connects sana-wm-pipeline to WorldBench?** The pipeline generates simulation data with varying quality (GT-pose vs. Default). If downstream policy performance on this data varies with pipeline quality — even holding PSNR/SSIM constant — that is a direct bridge between the two projects and the most compelling single experiment David could run right now. It makes both papers stronger.

---

## Tomorrow's Top 3 Priorities

1. **Commit the WorldBench/WMATS design doc to `Planning/WorldBench_Design.md`** — This is the fifth and final time this is listed as priority #1 before it becomes embarrassing. The specific, minimal content needed: (a) WMATS metric definition, (b) four evaluation conditions (Cosmos Predict 2.5, AHEAD, BiWM, no-WM baseline), (c) task spec (MuJoCo peg insertion via OpenEnv), (d) policy backbone (Diffusion Policy), (e) expected result (FVD rank ≠ WMATS rank). The June 13 pipeline work has given you a concrete empirical motivator: the PSNR/ATE decorrelation in sana-wm-pipeline is direct evidence for the FVD/WMATS decorrelation hypothesis. Use it as the opening motivation sentence. This should take 20 minutes. The competitive clock with `tigee1311/worldbench` has been running for 5 days.

2. **Decide what "next" means for sana-wm-pipeline** — The three-mode validation is complete and documented. The outstanding question is whether the next step is: (a) scale up to full DL3DV/OmniWorld datasets, (b) add a semantic evaluation layer beyond PSNR/SSIM, (c) integrate with the WorldBench experiment as the "simulation data quality axis," or (d) write this up as a standalone tool paper. Each of these is a different 4-6 week project. Committing even a 3-sentence decision note to `Planning/sana-wm-next-steps.md` is the minimum needed to prevent another week of undefined state.

3. **Run the no-WM Diffusion Policy baseline (first WMATS number)** — Set up OpenEnv + MuJoCo peg insertion + Diffusion Policy. Run with zero world model rollouts. The June 14 morning briefing called for "one number" by end of Sunday — that deadline has passed. The number is still the highest-priority experimental action because without it, WorldBench is a design doc, not a research project. GPU time: ~4 H100 hours. Even a partial run that reaches 5K steps with a preliminary number is a commit.
